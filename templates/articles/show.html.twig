{% extends 'base.html.twig' %}

{% block title %}{{ article.title }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>{{ article.title }}</h1>
                <p>{{ article.body }}</p>
                <hr>
                <a href="{{ path('article_list') }}">Go Back</a>
            </div>
        </div>
    </div>
{% endblock %}